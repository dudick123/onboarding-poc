# Auto-generated by Copier for tenant: acme
trigger:
  branches:
    include:
      - main

variables:
  imageRepo: acr.azurecr.io/acme/orders-api

stages:
  - stage: BuildAndPush
    jobs:
      - job: DockerBuildPush
        steps:
          - task: Docker@2
            inputs:
              command: build
              repository: $(imageRepo)
              tags: |
                $(Build.BuildId)
            displayName: 'docker build for acme'
          - task: Docker@2
            inputs:
              command: push
              repository: $(imageRepo)
              tags: |
                $(Build.BuildId)
            displayName: 'docker push for acme'
