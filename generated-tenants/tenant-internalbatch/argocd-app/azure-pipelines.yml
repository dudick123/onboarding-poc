# Auto-generated by Copier for tenant: internalbatch
trigger: none   # manually triggered only — this IS the change-control gate

stages:
  - stage: Sync
    jobs:
      - deployment: ArgoCDSync
        environment: 'argocd-sync-internalbatch-approval'   # requires human approval
        strategy:
          runOnce:
            deploy:
              steps:
                - script: argocd app sync internalbatch --prune
                  displayName: 'ArgoCD sync for internalbatch (requires human approval)'
