# Auto-generated by Copier for tenant: internalbatch
trigger:
  branches:
    include:
      - main

variables:
  imageRepo: acr.azurecr.io/internalbatch/worker

stages:
  - stage: BuildAndPush
    jobs:
      - job: DockerBuildPush
        steps:
          - task: Docker@2
            inputs:
              command: build
              repository: $(imageRepo)
              tags: |
                $(Build.BuildId)
            displayName: 'docker build for internalbatch'
          - task: Docker@2
            inputs:
              command: push
              repository: $(imageRepo)
              tags: |
                $(Build.BuildId)
            displayName: 'docker push for internalbatch'
