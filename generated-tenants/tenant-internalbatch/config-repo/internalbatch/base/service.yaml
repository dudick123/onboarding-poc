apiVersion: v1
kind: Service
metadata:
  name: internalbatch
  namespace: internalbatch
  labels:
    app: internalbatch
spec:
  selector:
    app: internalbatch
  ports:
    - port: 8080
      targetPort: 8080
