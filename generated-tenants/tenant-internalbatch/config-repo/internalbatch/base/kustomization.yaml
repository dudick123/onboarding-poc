apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: internalbatch-base

commonLabels:
  app.kubernetes.io/part-of: internalbatch
  platform.io/tenant: internalbatch
  platform.io/tier: small

resources:
  - deployment.yaml
  - service.yaml
  - external-secret.yaml
