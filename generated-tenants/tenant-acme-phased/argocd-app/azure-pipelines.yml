# Auto-generated by Copier for tenant: acme
trigger: none   # manually triggered only — this IS the change-control gate

stages:
  - stage: Sync
    jobs:
      - deployment: ArgoCDSync
        environment: 'argocd-sync-acme-approval'   # requires human approval
        strategy:
          runOnce:
            deploy:
              steps:
                - script: argocd app sync acme --prune
                  displayName: 'ArgoCD sync for acme (requires human approval)'
