# Auto-generated by Copier for tenant: acme
trigger:
  branches:
    include:
      - main

stages:
  - stage: ValidateManifests
    jobs:
      - job: KustomizeBuild
        steps:
          - script: |
              kustomize build acme/overlays/dev > /tmp/dev.yaml
              kustomize build acme/overlays/staging > /tmp/staging.yaml
              kustomize build acme/overlays/prod > /tmp/prod.yaml
            displayName: 'kustomize build all overlays'
