# Auto-generated by Copier for tenant: {{ tenant_slug }}
trigger: none   # manually triggered only — this IS the change-control gate

stages:
  - stage: Sync
    jobs:
      - deployment: ArgoCDSync
        environment: 'argocd-sync-{{ tenant_slug }}-approval'   # requires human approval
        strategy:
          runOnce:
            deploy:
              steps:
                - script: argocd app sync {{ tenant_slug }} --prune
                  displayName: 'ArgoCD sync for {{ tenant_slug }} (requires human approval)'
