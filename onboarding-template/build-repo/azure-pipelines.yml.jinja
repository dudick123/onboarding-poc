# Auto-generated by Copier for tenant: {{ tenant_slug }}
trigger:
  branches:
    include:
      - main

variables:
  imageRepo: {{ container_image }}

stages:
  - stage: BuildAndPush
    jobs:
      - job: DockerBuildPush
        steps:
          - task: Docker@2
            inputs:
              command: build
              repository: $(imageRepo)
              tags: |
                $(Build.BuildId)
            displayName: 'docker build for {{ tenant_slug }}'
          - task: Docker@2
            inputs:
              command: push
              repository: $(imageRepo)
              tags: |
                $(Build.BuildId)
            displayName: 'docker push for {{ tenant_slug }}'
