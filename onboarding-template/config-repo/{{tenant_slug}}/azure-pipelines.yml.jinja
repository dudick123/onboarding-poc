# Auto-generated by Copier for tenant: {{ tenant_slug }}
trigger:
  branches:
    include:
      - main

stages:
  - stage: ValidateManifests
    jobs:
      - job: KustomizeBuild
        steps:
          - script: |
              kustomize build {{ tenant_slug }}/overlays/dev > /tmp/dev.yaml
{% if "staging" in environments %}
              kustomize build {{ tenant_slug }}/overlays/staging > /tmp/staging.yaml
{% endif %}
{% if "prod" in environments %}
              kustomize build {{ tenant_slug }}/overlays/prod > /tmp/prod.yaml
{% endif %}
            displayName: 'kustomize build all overlays'
