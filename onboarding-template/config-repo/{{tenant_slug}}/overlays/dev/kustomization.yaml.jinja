apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: {{ tenant_slug }}-dev

bases:
  - ../../base

namePrefix: dev-

replicas:
  - name: {{ tenant_slug }}
    count: {% if "dev" == "prod" %}{{ "3" if resource_tier == "large" else "2" }}{% else %}1{% endif %}

images:
  - name: {{ container_image }}
    newTag: dev
